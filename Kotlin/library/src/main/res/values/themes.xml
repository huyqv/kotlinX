<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- App Theme. -->
    <style name="App" />

    <style name="App.Theme"
        parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimary</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:splitMotionEvents">false</item>
        <item name="android:windowEnableSplitTouch">false</item>
        <item name="android:statusBarColor">@color/colorPrimaryDark</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>
        <item name="android:windowActivityTransitions">true</item>
        <item name="android:navigationBarColor">@android:color/white</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="o_mr1">true
        </item>
    </style>

    <style name="App.Theme.Transparent"
        parent="App.Theme">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>

    <style name="App.Theme.AppBarOverlay"
        parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="App.Theme.PopupOverlay"
        parent="ThemeOverlay.AppCompat.Light" />

    <!-- Dialog. -->
    <style name="App.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowMinWidthMajor">100%</item>
        <item name="android:windowMinWidthMinor">100%</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="App.Dialog.Transparent" parent="App.Dialog">
        <item name="android:backgroundDimEnabled">false</item>
    </style>

    <style name="App.Dialog.FullScreen" parent="App.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowSoftInputMode">stateUnspecified|adjustPan</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>

    <style name="App.Dialog.FullScreen.Transparent" parent="App.Dialog.FullScreen">
        <item name="android:backgroundDimEnabled">false</item>
    </style>

    <style name="App.DialogAnim">
        <item name="android:windowEnterAnimation">@anim/vertical_enter</item>
        <item name="android:windowExitAnimation">@anim/vertical_exit</item>
    </style>


</resources>